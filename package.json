{
  "name": "sri-sai-college-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Sri Sai PU and Degree College Management System - frontend and backend in one repository, deployable as a single Vercel project.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm start --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "docx": "^9.7.1",
    "express": "^4.22.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.3.0",
    "pdfkit": "^0.20.2",
    "pg": "^8.23.0"
  }
}
